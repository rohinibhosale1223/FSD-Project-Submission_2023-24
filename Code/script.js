// script.js

// Example: Add a click event handler to a button with the id "myButton"
const myButton = document.getElementById('myButton');

if (myButton) {
  myButton.addEventListener('click', () => {
    alert('Button clicked!');
  });
}
